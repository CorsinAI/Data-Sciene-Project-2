# This package contains all image-based (static picture) sign language recognition scripts.
# It is intentionally separate from the video-based pipeline in src/training/
# to avoid naming conflicts and keep the two approaches clearly distinct.
